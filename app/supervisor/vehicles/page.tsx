import Link from "next/link";
import { prisma } from "@/lib/prisma";
import { requireAdminOrSupervisor } from "@/lib/permissions";
export default async function SupervisorVehiclesPage() { await requireAdminOrSupervisor(); const vehicles = await prisma.vehicle.findMany({ orderBy: [{ displayOrder: "asc" }, { name: "asc" }] }); return <div className="mx-auto max-w-7xl"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold text-primary">Fleet operations</p><h1 className="mt-1 text-3xl font-semibold">Vehicles</h1></div><Link href="/supervisor/vehicles/new" className="min-h-11 rounded-full bg-primary px-6 py-3 text-center text-sm font-semibold">Add Vehicle</Link></div><div className="mt-7 space-y-4">{vehicles.map((v) => <article key={v.id} className="grid gap-3 rounded-2xl border border-border bg-white p-5 sm:grid-cols-[1fr_1fr_auto] sm:items-center"><div><h2 className="font-semibold">{v.name}</h2><p className="text-sm text-muted">{v.vehicleNumber} · {v.vehicleType}</p></div><p className="text-sm">{v.isActive ? "Active" : "Inactive"} · {v.isPublic ? "Public" : "Hidden"}</p><Link href={`/supervisor/vehicles/${v.id}`} className="min-h-11 rounded-xl border border-border px-4 py-3 text-center text-sm font-semibold">Manage</Link></article>)}</div></div>; }
