import Link from "next/link";
import { OperationCard } from "@/components/operation-card";
import { prisma } from "@/lib/prisma";

export async function OperationsPreview() {
  const operations = await prisma.operation.findMany({ where: { isPublished: true }, orderBy: { operationDate: "desc" }, take: 3, select: { id: true, title: true, shortDescription: true, serviceType: true, area: true, operationDate: true, photos: { where: { taskPhoto: { isApprovedForPublic: true } }, orderBy: { displayOrder: "asc" }, take: 1, select: { id: true } } } });
  return <section aria-labelledby="operations-preview-title" className="operations-preview bg-light-background px-6 py-20 sm:py-24"><div className="mx-auto max-w-7xl"><div className="mx-auto max-w-2xl text-center"><p className="text-sm font-semibold uppercase tracking-[0.2em] text-foreground">Recent Operations</p><h2 id="operations-preview-title" className="mt-3 text-3xl font-semibold tracking-tight text-foreground sm:text-4xl">Serving the Community Through Action</h2><div className="section-ornament" aria-hidden="true"><span /></div><p className="mx-auto mt-4 max-w-xl leading-7 text-muted">Carefully reviewed stories about services carried out by Janaza Welfare Society.</p></div>{operations.length ? <div className="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{operations.map((operation) => <OperationCard key={operation.id} operation={operation} />)}</div> : <p className="mx-auto mt-12 max-w-2xl rounded-2xl border border-border bg-white p-8 text-center text-muted">No public operations are available yet.</p>}<div className="mt-12 text-center"><Link href="/operations" className="inline-flex min-h-12 items-center justify-center rounded-full border border-primary bg-white px-6 py-3 text-sm font-semibold text-foreground shadow-sm hover:shadow-[0_9px_25px_rgba(69,232,205,0.22)]">View All Operations</Link></div></div></section>;
}
